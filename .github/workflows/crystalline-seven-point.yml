name: crystalline-seven-point-certificate

on:
  push:
    branches: [main]
    paths:
      - "verifier/**"
      - ".github/workflows/crystalline-seven-point.yml"
  pull_request:
    branches: [main]
    paths:
      - "verifier/**"
      - ".github/workflows/crystalline-seven-point.yml"
  workflow_dispatch:
    inputs:
      run_interval:
        description: "Run the full six-gap interval certificate"
        required: false
        default: false
        type: boolean
      workers:
        description: "Parallel verifier workers"
        required: false
        default: "4"

jobs:
  fast:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout crystalline research repo
        uses: actions/checkout@v4
      - name: Checkout upstream verifier
        uses: actions/checkout@v4
        with:
          repository: trmdy/zeta-simple-zeros-673137
          path: upstream
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install candidate into upstream verifier
        run: |
          python verifier/apply_algebraic_frequency.py
          cp verifier/crystal_design.py upstream/src/zeta_ext/crystal_design.py
          cp verifier/test_crystal_design.py upstream/tests/test_crystal_design.py
          cp verifier/crystal_verify.py upstream/crystal_verify.py
      - name: Install pinned verifier dependencies
        run: python -m pip install -e upstream
      - name: Run upstream and crystalline tests
        run: |
          cd upstream
          python -m unittest -v tests/test_zeta_ext.py tests/test_crystal_design.py
      - name: Run rigorous fast analytic gates
        run: |
          cd upstream
          python crystal_verify.py fast

  targeted-refinement:
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    needs: fast
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout crystalline research repo
        uses: actions/checkout@v4
      - name: Checkout upstream verifier
        uses: actions/checkout@v4
        with:
          repository: trmdy/zeta-simple-zeros-673137
          path: upstream
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install candidate and focused-box verifier extension
        run: |
          python verifier/apply_algebraic_frequency.py
          cp verifier/crystal_design.py upstream/src/zeta_ext/crystal_design.py
          cp verifier/targeted_refine.py upstream/targeted_refine.py
      - name: Install pinned verifier dependencies
        run: python -m pip install -e upstream
      - name: Recheck parent at grid 500
        run: |
          set -o pipefail
          cd upstream
          python targeted_refine.py --grid 500 2>&1 | tee targeted-grid500.txt
      - name: Cross-check parent at grid 1000
        run: |
          set -o pipefail
          cd upstream
          python targeted_refine.py --grid 1000 2>&1 | tee targeted-grid1000.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: targeted-refinement
          path: |
            upstream/targeted-grid500.txt
            upstream/targeted-grid1000.txt

  grid250-collector:
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    needs: fast
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout crystalline research repo
        uses: actions/checkout@v4
      - name: Checkout upstream verifier
        uses: actions/checkout@v4
        with:
          repository: trmdy/zeta-simple-zeros-673137
          path: upstream
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install collector-enabled crystalline verifier
        run: |
          python verifier/apply_algebraic_frequency.py
          cp verifier/crystal_design.py upstream/src/zeta_ext/crystal_design.py
          cp verifier/grid250_collect_and_refine.py upstream/grid250_collect_and_refine.py
      - name: Install pinned verifier dependencies
        run: python -m pip install -e upstream
      - name: Exhaust grid 250 and refine every terminal miss
        run: |
          set -o pipefail
          cd upstream
          python grid250_collect_and_refine.py 2>&1 | tee grid250-collector.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: grid250-collector
          path: |
            upstream/grid250-collector.txt
            upstream/grid250-unresolved.json
            upstream/grid500-unresolved.json
            upstream/grid1000-unresolved.json
          if-no-files-found: ignore

  interval:
    if: github.event_name == 'workflow_dispatch' && inputs.run_interval
    needs: fast
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout crystalline research repo
        uses: actions/checkout@v4
      - name: Checkout upstream verifier
        uses: actions/checkout@v4
        with:
          repository: trmdy/zeta-simple-zeros-673137
          path: upstream
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install candidate into upstream verifier
        run: |
          python verifier/apply_algebraic_frequency.py
          cp verifier/crystal_design.py upstream/src/zeta_ext/crystal_design.py
          cp verifier/test_crystal_design.py upstream/tests/test_crystal_design.py
          cp verifier/crystal_verify.py upstream/crystal_verify.py
      - name: Install pinned verifier dependencies
        run: python -m pip install -e upstream
      - name: Run full interval certificate
        run: |
          set -o pipefail
          cd upstream
          python crystal_verify.py main --grid 4000 --workers "${{ inputs.workers }}" 2>&1 | tee crystalline-seven-grid4000.txt
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: crystalline-seven-grid4000
          path: upstream/crystalline-seven-grid4000.txt
